# Исправление ошибки "Ошибка продления" в модалке "Продление доступа"

## Что было сделано

### 1. ✅ Детальное логирование ошибок

**Изменения:**
- Добавлены детальные `console.log` с точными ошибками от Supabase
- Логирование на сервере (API) и клиенте (страница админки)
- Ошибки теперь возвращаются с полной информацией: `error`, `code`, `details`, `hint`

**Где смотреть:**
1. **Консоль браузера** (F12 → Console):
   ```
   ❌ SAVE EXTENSION FAILED:
   - status: 500
   - error: "Database error: ..."
   - code: "PGRST..."
   - details: "..."
   - hint: "..."
   ```

2. **Логи Vercel** (или сервера):
   ```
   ❌ Update organization ERROR:
   - error: { ... }
   - code: "PGRST..."
   - message: "..."
   - details: "..."
   - hint: "..."
   ```

### 2. ✅ Проверка обязательных полей

**Валидация добавлена:**
- `org_id` — обязательно
- `subscription_status` — если указан, должен быть один из: `none`, `trial`, `active`, `manual`, `expired`
- Проверка существования организации (404 если не найдена)

### 3. ✅ SQL-скрипты для диагностики

**Созданы файлы:**

#### `CHECK_ORGANIZATIONS_TABLE.sql`
Проверяет:
- Структуру таблицы `organizations`
- RLS политики
- Constraint на `subscription_status`
- Существующие данные

#### `FIX_ORGANIZATIONS_RLS.sql`
Исправляет RLS политики:
- Админы получают полный доступ
- Обычные пользователи могут читать свою организацию
- Владельцы могут обновлять свою организацию

---

## Как найти причину ошибки

### Шаг 1: Проверьте консоль браузера

1. Откройте **DevTools** (F12)
2. Перейдите на вкладку **Console**
3. Откройте модалку "Продление доступа"
4. Заполните форму и нажмите "Сохранить"
5. Ищите сообщения:
   ```
   === SAVE EXTENSION ===
   Org ID: ...
   Plan: ...
   Features: {...}
   Subscription Update: {...}
   
   Response status: 500
   Response data: {"error": "...", "code": "...", "details": "...", "hint": "..."}
   
   ❌ SAVE EXTENSION FAILED: {...}
   ```

6. **Скопируйте всё содержимое ошибки** и отправьте мне

### Шаг 2: Проверьте логи Vercel

1. Зайдите в **Vercel Dashboard**
2. Откройте ваш проект → **Logs**
3. Отфильтруйте по `/api/admin/organizations/features`
4. Ищите записи с префиксом `❌`
5. **Скопируйте полный лог** и отправьте мне

### Шаг 3: Проверьте структуру таблицы

Запустите в **Supabase SQL Editor**:

```sql
-- Скопируйте содержимое CHECK_ORGANIZATIONS_TABLE.sql
```

**Что проверяем:**
- Есть ли поля `subscription_status`, `subscription_expires_at`, `features`, `plan`?
- Включен ли RLS?
- Какие политики настроены?

### Шаг 4: Проверьте RLS политики

Если ошибка связана с правами доступа (например, "new row violates row-level security policy"):

```sql
-- Скопируйте содержимое FIX_ORGANIZATIONS_RLS.sql
```

**Это исправит:**
- Даст админам полный доступ к таблице `organizations`
- Настроит правильные политики для обычных пользователей

---

## Частые ошибки и решения

### Ошибка 1: "Organization not found"

**Причина:** Организация с указанным ID не существует

**Решение:**
```sql
-- Проверьте, существует ли организация
SELECT id, name FROM organizations WHERE id = 'your-org-id';

-- Если нет, создайте организацию или используйте правильный ID
```

### Ошибка 2: "Invalid subscription_status"

**Причина:** Неправильное значение `subscription_status`

**Решение:**
Убедитесь, что значение одно из: `none`, `trial`, `active`, `manual`, `expired`

### Ошибка 3: "new row violates row-level security policy"

**Причина:** RLS политики блокируют обновление

**Решение:**
1. Запустите `FIX_ORGANIZATIONS_RLS.sql` в Supabase SQL Editor
2. Убедитесь, что ваш пользователь есть в таблице `admin_users`:
   ```sql
   SELECT * FROM admin_users WHERE user_id = auth.uid();
   ```
3. Если нет, добавьте себя:
   ```sql
   INSERT INTO admin_users (user_id, email, full_name, role)
   VALUES (
     auth.uid(),
     'your-email@example.com',
     'Your Name',
     'super_admin'
   );
   ```

### Ошибка 4: "column does not exist"

**Причина:** Таблица `organizations` не имеет нужных полей

**Решение:**
Запустите миграцию для добавления недостающих полей:
```sql
-- Добавить subscription_status (если нет)
ALTER TABLE organizations 
ADD COLUMN IF NOT EXISTS subscription_status TEXT DEFAULT 'none'
CHECK (subscription_status IN ('none', 'trial', 'active', 'manual', 'expired'));

-- Добавить subscription_expires_at (если нет)
ALTER TABLE organizations 
ADD COLUMN IF NOT EXISTS subscription_expires_at TIMESTAMPTZ;

-- Добавить features (если нет)
ALTER TABLE organizations 
ADD COLUMN IF NOT EXISTS features JSONB DEFAULT '{}'::jsonb;

-- Добавить plan (если нет)
ALTER TABLE organizations 
ADD COLUMN IF NOT EXISTS plan TEXT DEFAULT 'demo';
```

### Ошибка 5: "PGRST116" (Not Found)

**Причина:** PostgREST не может найти запись

**Решение:**
1. Проверьте, что `org_id` правильный
2. Проверьте RLS политики (возможно, блокируют чтение)
3. Запустите:
   ```sql
   -- Временно отключить RLS для проверки
   ALTER TABLE organizations DISABLE ROW LEVEL SECURITY;
   
   -- Попробуйте сохранить снова
   
   -- Включите обратно
   ALTER TABLE organizations ENABLE ROW LEVEL SECURITY;
   ```

---

## Тестирование исправления

### 1. После деплоя

1. Откройте консоль браузера (F12)
2. Зайдите в админ-панель → Подписки
3. Нажмите "Продлить" на любой организации
4. Заполните форму и нажмите "Сохранить"

**Ожидаемое поведение:**
- ✅ Если успешно: "Доступ продлён" (toast), модалка закрывается
- ❌ Если ошибка: Детальное сообщение в toast (10 секунд) с точной ошибкой

### 2. Проверка логов

В консоли браузера должны быть:
```
=== SAVE EXTENSION ===
Org ID: ...
Plan: ...
Features: {...}
Subscription Update: {...}

📦 Update data prepared: {...}
📦 Full update data: {...}

✅ Organization updated successfully: {...}

Response status: 200
Response data: {"success": true, "organization": {...}}
```

---

## Что делать дальше

1. **Откройте консоль браузера** и попробуйте сохранить продление
2. **Скопируйте все логи** из консоли (особенно с ❌)
3. **Отправьте мне:**
   - Скриншот консоли с ошибкой
   - Полный текст ошибки
   - Результат выполнения `CHECK_ORGANIZATIONS_TABLE.sql`

Я смогу точно определить причину и исправить!

---

## Статус

- ✅ Детальное логирование добавлено
- ✅ Валидация полей добавлена
- ✅ SQL-скрипты для диагностики созданы
- ✅ Улучшена обработка ошибок на клиенте
- ✅ Улучшена обработка ошибок на сервере
- ✅ Код запушен в main
- 🚀 Автоматический деплой через Vercel

**Последнее обновление:** 2026-02-26
