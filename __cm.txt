fix: service_type NOT NULL on visit update + optimistic UUID guards

Visits:
- api/visits/[id]/route.ts: always read+preserve service_type from DB before UPDATE
  prevents "null value in column service_type" when service_id not sent

Clients UUID:
- useClients.ts: useClient enabled guard - skip query for optimistic IDs
- UnifiedVisitDialog.tsx: guard clientId before POST/PUT visit
- api/clients/[id]/route.ts: UUID regex guard in GET+PUT before Supabase call
- validations.ts: strictUuid helper, updateClientSchema, createVisitSchema uses strictUuid
