# Финальное исправление ошибки [object Object]

## Что было сделано

✅ **1. Глобальный обработчик ошибок**
- Добавлен в `QueryProvider`
- Ловит все unhandled promise rejections
- Предотвращает показ error overlay

✅ **2. Обновлены настройки React Query**
```typescript
{
  retry: false,              // Не повторять запросы
  throwOnError: false,       // Не выбрасывать ошибки
  useErrorBoundary: false,   // Не использовать error boundary
}
```

✅ **3. Хуки возвращают пустые данные вместо ошибок**
- `useAdCampaigns()` → возвращает `[]` при ошибке
- `useAdStats()` → возвращает дефолтные значения при ошибке
- Все ошибки логируются в консоль

---

## Что делать если ошибка осталась

### Шаг 1: Очистите кэш браузера

1. Откройте DevTools (F12)
2. Во вкладке Network поставьте галку **"Disable cache"**
3. **Hard refresh:** Ctrl+Shift+R (Windows) или Cmd+Shift+R (Mac)

### Шаг 2: Перезапустите dev server

```bash
# Остановите (Ctrl+C)
# Удалите кэш Next.js
rm -rf .next

# Запустите заново
npm run dev
```

### Шаг 3: Проверьте что таблица существует

```sql
-- В Supabase SQL Editor:
SELECT COUNT(*) FROM ad_campaigns;
```

Если ошибка "relation does not exist":
1. Выполните `supabase/schema-v2.sql`
2. Или `supabase/quick-fix-ad-campaigns.sql`

---

## Что вы должны видеть теперь

**Если таблица не создана:**
- Страница загрузится
- Показ "אין קמפיינים פעילים" (пустая таблица)
- Карточки статистики покажут 0
- **НЕ будет ошибки [object Object]**

**В консоли браузера:**
```
Ad campaigns query error: { code: 'PGRST204', ... }
```

**В терминале (если есть):**
```
[Unhandled Promise Rejection]: ...
```

---

## Если ошибка ВСЁЩЕ появляется

### Проверка 1: Посмотрите что именно в ошибке

Откройте DevTools → Console → найдите полный текст ошибки

### Проверка 2: Проверьте что изменения применились

```typescript
// В QueryProvider.tsx должно быть:
throwOnError: false,
useErrorBoundary: false,

// И useEffect с:
window.addEventListener('unhandledrejection', handleError)
```

### Проверка 3: Временное решение - НЕ открывайте страницу /admin/ads

Используйте другие страницы админки:
- `/admin` — дашборд
- `/admin/organizations` — организации  
- `/admin/billing` — биллинг
- `/admin/settings` — настройки

---

## Быстрая диагностика

```bash
# 1. Проверить что файлы обновлены
grep "unhandledrejection" src/components/providers/QueryProvider.tsx
# Должен вернуть строку с addEventListener

# 2. Проверить что useAdCampaigns возвращает []
grep "return \[\]" src/hooks/useAdmin.ts | wc -l
# Должно быть несколько строк

# 3. Перезапустить с чистым кэшем
rm -rf .next && npm run dev
```

---

## Окончательное решение

Если ничего не помогает:

**Временно закомментируйте страницу ads:**

```typescript
// src/app/admin/ads/page.tsx
export default function AdsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold">דף זה בפיתוח</h1>
      <p>אנא צור את הטבלה ad_campaigns קודם</p>
    </div>
  )
}
```

---

**После создания таблицы `ad_campaigns` всё заработает!** 🎉
