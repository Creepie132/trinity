fix: webhook state machine ON CONFLICT DO UPDATE + signature verification

КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ:
  DO NOTHING → DO UPDATE SET status=EXCLUDED.status, updated_at=now()
  pending→success переход теперь корректно обрабатывается.
  Деньги не зависают в pending при ретрае от Tranzila.

src/app/api/gateway/webhook/route.ts (новый)
  - Читает raw body через request.text() (а НЕ json()) для корректной
    signature verification (хеши совпадают с raw телом запроса)
  - Использует verifyTranzilaSignature() + validateTranzilaTerminal() из lib/
  - upsert с onConflict:'gateway,gateway_ref', ignoreDuplicates:false
    → DO UPDATE: pending→success, pending→failed — апдейт проходит
    → success→success (retry Tranzila): идемпотентен
  - Апдейт payment_links по idempotency_key (cField2) через .neq('status','paid')
    защита от нелегитимного отката paid→pending
  - Возвращает 200 при ошибке подписи (Tranzila не ретраит зацикленно)
  - Логи с префиксом [PAYMENTS_MODULE_ERROR] и [PAYMENTS_MODULE]

supabase/migrations/20260515_payments_module_patch.sql
  - ALTER TABLE gateway_transactions ADD COLUMN updated_at (для upsert)
  - Исправлен комментарий UNIQUE constraint: DO UPDATE вместо DO NOTHING
  - Задокументирована стейт-машина в SQL комментарии

middleware.ts
  - /api/gateway/webhook добавлен в PUBLIC_PATH_PREFIXES (Tranzila шлёт без авторизации)
