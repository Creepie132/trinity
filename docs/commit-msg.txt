feat: org_type toggle in Admin Panel + PATCH /api/admin/organizations/[orgId]

src/app/admin/organizations/page.tsx
  - OrgTypeToggle компонент (amber цвет, тот же паттерн что QuickMode/Storefront)
  - Тоггл показывает текущий тип: Trinity CRM / Payments Only
  - Confirm диалог предупреждает что пользователь должен перелогиниться
  - org_type отображается в detail panel рядом с другими тогглами

src/app/api/admin/organizations/[orgId]/route.ts
  - PATCH handler: обновляет org_type (trinity | payments_only)
  - Whitelist allowedFields — только явно разрешённые поля
  - Валидация enum значений перед записью в БД
  - Service role клиент для обхода RLS

src/app/api/admin/subscriptions-list/route.ts
  - Добавлен org_type в SELECT запрос — приходит в список орг на фронт
