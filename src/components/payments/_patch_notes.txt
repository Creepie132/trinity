PATCH_TARGET: UnifiedPaymentDialog.tsx

Changes needed (line numbers are approximate):
1. After PaymentDetailForms import — add InstallmentForm import
2. PaymentMethod type — add 'installment'
3. Step type — add 'installment-form'
4. METHODS array — add installment entry
5. METHOD_ICONS — add installment icon
6. I18N — add installment strings
7. handleSelectMethod stepMap — add installment
8. currentMethod detection — add installment-form
9. State — add installmentConfig, clientToken
10. handleSubmit — add installment branch
11. renderStep — add installment-form step
